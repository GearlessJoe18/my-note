function debounce(fn, delay = 300, immediate = false) {
  let timer = null; // 保存定时器标识

  return function (...args) {
    // 清除之前的定时器，重新计时
    if (timer) clearTimeout(timer);

    // 立即执行版：第一次触发时立即执行，后续触发重新计时
    if (immediate) {
      const callNow = !timer; // 首次触发时timer为null，callNow为true
      timer = setTimeout(() => {
        timer = null; // 延迟结束后重置timer
      }, delay);
      if (callNow) fn.apply(this, args);
    } else {
      // 非立即执行版：触发后延迟执行
      timer = setTimeout(() => {
        fn.apply(this, args);
      }, delay);
    }
  };
}