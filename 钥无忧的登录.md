查看钥无忧

router.js   （添加）

main.js引入 （添加permission）

permission.js拦截器
（不要api 、isStanby  、defaultSettings决定是否ukey版本 对应settings，但是只要process.env.VUE_APP_ISUKEY，不要ai版本）

添加env环境配置吧

firstpage
isUkey -（1要ukey）--isgetkeytimer（开定时器拿ukey） <-apiGetLocalKey连接js脚本，定时器也能清，都有，就用原版逻辑
密钥给 store.state.sKey和localStorage里的projectkey

--getDatabaseStat不要  --router push（把要跳转的页面从routerdata取出来，没有就是去首页）

不需要ukey？这个逻辑要不要（问长涛老师）

home view--组件header -- 登录（v-if的token）-- eldialog --组件log
重点查看token的来去

log -- el-form --validate -- login

login --loading缓冲 -- 数据给store -- （注释代码）defaultSettings判断ukey版本，先拿ukey（不是开定时取），然后调login的api，then调定时器拿key
然后设置token，（函数）
先确定res不为空，
然后用utils的方法localstorage set token（或者cookies set），
然后用utils set userinfo到localstorage，
然后触发mutation往store里面存token，
然后直接把account_type 存入localstorage，
最后location.reload()刷新页面

res出store，
如果res不为空，那说明成功了 于是emit登录状态

往localkey{username 和 password 的名字}（防止命名冲突，便于统一管理）里面存信息（remember存localstorage，username和pwd存cookies里面，用compilestr加密）


刷新页面后，父组件重新执行，v-if的token（从state）



