查看钥无忧

router.js   （添加）

main.js引入 （添加permission）

permission.js拦截器
（不要api 、isStanby  、defaultSettings决定是否ukey版本 对应settings，但是只要process.env.VUE_APP_ISUKEY，不要ai版本）

添加env环境配置吧

1. 调用 `request(config)` → axios 马上创建一个 Promise（这时还没发完请求）。
2. 请求拦截器：改 `config`（加 token、改 baseURL 等），然后真正发 HTTP。
3. 浏览器 / Node 把请求发到服务器，等响应。
4. 响应拦截器：拿到 `response`，读 `response.data.status`、转码、`return res` 等。
5. Promise 结束：根据拦截器最终是 `return` 正常值还是 `Promise.reject`，这次请求的 Promise resolve 或 reject。调用方 `.then` / `.catch` 才会执行。

- 成功回调里：你们对 401、`-1/0`、411–415 等都是在拦截器内处理，最后多数是 `return res`（或 `return utf8Data` / `jsonData`），没有把业务错 `Promise.reject` 出去，所以 `.catch` 收不到这些业务错误。
- 失败回调里（第二个参数）：`return Promise.reject(error)` 还在，所以 HTTP 非 2xx（默认）、超时、断网 等 axios 会进 error 分支时，仍然会 reject，调用方可以 `.catch`。


firstpage
isUkey（api） -（1要ukey）--isgetkeytimer（开定时器执行apiGetLocalKey） <-apiGetLocalKey返回promise，getLocalKey（api）连接js脚本


getLocalKey的
.then  密钥setProjectKey（func）给 store.state.sKey和projectke（localStorage）
发resolve让apiGetLocalKey的promise返回.then
.catch removeProjectKey(func) 



--getDatabaseStat不要  --router push（把要跳转的页面从routerdata取出来，没有就是去首页）

不需要ukey？这个逻辑要不要（问长涛老师）

home view--组件header -- 登录（v-if的token）-- eldialog --组件log
重点查看token的来去

log -- el-form --validate -- login

login --loading缓冲 -- 数据给store -- （注释代码）defaultSettings判断ukey版本，先拿ukey（不是开定时取），
然后调login的api，then调定时器拿key
然后设置token，（函数）
先确定res不为空，
然后用utils的方法localstorage set token（或者cookies set），
然后用utils set userinfo到localstorage，
然后触发mutation往store里面存token，
然后直接把account_type 存入localstorage，
最后location.reload()刷新页面

res出store，
如果res不为空，那说明成功了 于是emit登录状态

往localkey{username 和 password 的名字}（防止命名冲突，便于统一管理）里面存信息（remember存localstorage，username和pwd存cookies里面，用compilestr加密）


刷新页面后，父组件重新执行，v-if的token（从state里面拿到的）有了，所以显示登录后的页面



