查看钥无忧

router.js   （添加）

main.js引入 （添加permission）

permission.js拦截器
（不要api 、isStanby  、defaultSettings要 对应settings，但是只要process.env.VUE_APP_ISUKEY）

添加env环境配置吧

firstpage
isUkey -（1要ukey）--isgetkeytimer（开定时器拿ukey） <-apiGetLocalKey连接js脚本，定时器也能清，都有，就用原版逻辑
密钥给 store.state.sKey和localStorage里的projectkey

--getDatabaseStat不要  --router push（把要跳转的页面从routerdata取出来，没有就是去首页）

不需要ukey？这个逻辑要不要（问长涛老师）

home view--组件header -- 登录（v-if的token）-- eldialog --组件log
重点查看token的来去

log -- el-form --validate -- login

login --loading缓冲 -- 数据给store -- 
