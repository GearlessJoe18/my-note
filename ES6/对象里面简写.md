const obj = {
	 // 完整写法：key: function() {} 
	 created: function() { 
		 console.log('我是 created') 
	 } 
 }
 
 es6简写
 const obj = { 
	 created() { 
		 console.log('我是 created') 
	 } 
 }
 
 比如在vue配置项中经常见到的
 data（）{
	 return{
		 //返回一个obj
	 }
 }
 
 on