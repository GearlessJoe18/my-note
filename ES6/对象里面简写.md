const obj = {
	 // 完整写法：key: function() {} 
	 created: function() { 
		 console.log('我是 created') 
	 } 
 }
 
 es6简写
 const obj = { 
	 created() { 
		 console.log('我是 created') 
	 } 
 }
 
 
 
 比如在vue配置项中经常见到的
 data（）{
	 return{
		 //返回一个obj
	 }
 }
 
  mounted() {
	func()
  }
  
  因为这些vue配置项都是包在一个对象里面的，所以我必须思考他是在对象里面的语法
  
  method:{}
  method就是一个对象里的属性