### **Set** - 不重复的值集合

一个不包含重复值的数组

`// 创建`
`const set = new Set([1, 2, 3, 3, 3])`  

`//set(3)=[1,2,3]`

`// 常用方法`
`set.add(4)           // 添加`
`set.delete(1)        // 删除`
`set.has(2)           // 检查是否存在 → true`
`set.size             // 大小 → 3`
`set.clear()          // 清空`