querySelectorAll（选择器）放入数组中

	axios({
                method:'GET',
                url:'http://localhost:3000/posts'
         })
         返回promise对象 .then接收

配置默认对象
	`axios.defaults.method='GET'`
	`axios.defaults.baseURL='http://localhost:3000'`

