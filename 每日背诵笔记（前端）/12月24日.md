img的onload属性：
创建一个 HTMLImageElement 实例（img）；
将你写的箭头函数保存到这个实例的 onload 属性中
浏览器向 img.src 对应的 URL 发起 HTTP/HTTPS 请求，返回状态码为 200 OK（或 304 缓存命中）；
检查 img 对象的 onload 属性是否有绑定函数（即你之前存的箭头函数 / 普通函数）；
如果有，主动调用这个函数（相当于浏览器帮你执行 img.onload()）；
函数执行时，会传入一个 Event 对象（可选）。