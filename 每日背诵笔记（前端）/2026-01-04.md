const ADD_ITEMNUM = 'ADD_ITEMNUM'
**用「变量引用」替代「硬编码字符串」，
把「隐性的拼写错误」转化为「显性的语法错误」，
把「多处修改」简化为「一处修改」**

ES6 允许在对象字面量中，用 `[表达式]` 作为属性名 / 方法名，表达式的**执行结果**会成为最终的属性名：

定时器函数有返回值，返回的是浏览器分配的唯一标识！
let n=0
        let v1 = setInterval(()=>{
            console.log(n++);
            if(n==5){
            clearInterval(v1)
            }
        },1000)