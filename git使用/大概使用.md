git clone下来的是整个仓库，不过默认分支是main，所以在本地仓库中只能看到main中的内容

git branch -a  显示远程和本地的所有分支

git branch只显示本地分支

git checkout 分支（去掉remotes\origin）
此时本地仓库会更新成该分支下的代码

git checkout -b dev_daizhichun 创建新分支并跳转

git pull 、git add 、git commit 、git push

合并好像不是我能做的merge