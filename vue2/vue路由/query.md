`query` 是**查询参数**（URL 中末尾 `?` 后面的键值对）

Vue路由把query动态参数传递注入到path中，并以问号开头&符号隔开键值对的形式。

不需要占位

可以被`this.$route.query.id`来访问到


可以运用  `params:{id:300}`  来
**传递 params 动态路径参数**：将 `id: 300` 作为路径参数，注入到路由配置中定义的 `/:id` 占位符位置