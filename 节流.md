function throttle(fn, interval = 300) {
  let timer = null;

  return function (...args) {
    if (!timer) {
      timer = setTimeout(() => {
        fn.apply(this, args);
        timer = null; // 执行后清空定时器
      }, interval);
    }
  };
}

触发时若没有待执行的定时器，就创建定时器，到时间执行回调并清空定时器，核心是「固定间隔内仅执行一次」