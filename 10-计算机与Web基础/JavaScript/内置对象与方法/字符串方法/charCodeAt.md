`const str = 'Hello'`
`console.log(str.charCodeAt(0))  // 72  （H 的编码）`
`console.log(str.charCodeAt(1))  // 101 （e 的编码）`
`console.log(str.charCodeAt(4))  // 111 （o 的编码）`
`console.log(str.charCodeAt(10)) // NaN （索引超出范围）`