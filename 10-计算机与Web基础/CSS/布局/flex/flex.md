- 容器（父元素）：`display: flex`
- 项目（子元素）：容器内的直接子元素






- `flex-wrap`：是否换行（wrap 解决溢出）

`.box{`
  `flex-wrap: nowrap | wrap | wrap-reverse;`
`}`






- `flex: 1`：占满剩余空间（均分列 / 行）
- `align-self`：单独调整某项目的交叉轴对齐