1、自定义组件的v-model：
- **子组件接收 `value` 这个 prop**（父组件通过 `v-model` 传的值，会被子组件以 `props: ['value']` 接收）；
- **子组件触发 `input` 事件**（子组件通过 `this.$emit('input', 新值)` 向父组件传新值，父组件的 `v-model` 变量会自动更新）。

2、element ui 中的 form 表单的 ref 获取到 dom 可以进行validate操作